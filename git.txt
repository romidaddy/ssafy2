링크 카피

power shell에서 붙여넣기 하면 다운됨

choco install git

a

exit

재접속

vscode도 재접속

작업한 것들 있는 곳에서 여기서 열기 (vscode)

터미널 열기 Ctrl + ~

PS C:\Users\multicampus> cd C:\Users\multicampus\Desktop\git.hub

git init

git config --global user.name "내 아이디" YeongJin-Yang

git config --global user.email "내 이메일"

git add -A

git status

git commit -m "커밋내용..예를들면 210817"

git hub 사이트 접속

new repository 만듥기

git remote add origin 만든 주소 적고

git push -u origin master

다시 불러오기

Shift + 우클릭 

여기에 Power Shell 열기

git hub에 불러올 파일 주소 복사

git clone 여기다 붙여넣기